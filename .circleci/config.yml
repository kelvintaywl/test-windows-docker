version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  echo:
    executor:
      name: win/default
      shell: powershell.exe
    steps:
      - run:
          name: test Linux containers
          command: |
            docker run --rm busybox echo hello_world
            docker image ls

workflows:
  main:
    jobs:
      - echo
